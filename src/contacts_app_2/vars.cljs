(ns contacts-app-2.vars)

(def states #{:reading
              :updating
              :deleting
              :creating
              :searching-by-name
              :searching-by-group})

(def state {})
